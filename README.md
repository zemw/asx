A script to download articles from ASX (aisixiang.com), provided the URL of the author's column. 

Use `pandoc` to combine multiple files into a single output:

```bash
pandoc *.html -o merged.docx
```